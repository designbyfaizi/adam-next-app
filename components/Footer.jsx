import styles from '../styles/Footer.module.scss'

const Footer = () => {
    return ( 
    <footer className={`${styles.footer}`}>
        <h1>
            ADAM
        </h1>
    </footer> );
}
 
export default Footer;